<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme: no action bar so WebView fills the full screen -->
    <style name="Theme.FocusFine" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorAccent">@color/black</item>
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- Splash screen theme: shown while app loads, then replaced by Theme.FocusFine -->
    <style name="Theme.FocusFine.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.FocusFine</item>
    </style>

    <!-- Overlay lock screen: transparent so the locked app shows behind -->
    <style name="Theme.FocusFine.Overlay" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
